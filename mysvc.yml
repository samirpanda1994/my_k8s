apiVersion: v1
kind: Service
metadata:
  name: myjenkins-svc
  lables:
    app: myjenkins-pod
spec:
  type: NodePort
  ports:
  -port: 8080
   nodePort: 30001
   protocol: tcp
  selector:
      app: myjenkins-pod
